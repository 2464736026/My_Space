# 前端部署指南

## 📌 当前状态

✅ 后端API已部署：https://my-space-beryl.vercel.app
✅ 前端代码已更新API地址
✅ 前端配置已优化

## 🚀 部署前端到Vercel

### 方法1：通过Vercel Dashboard（推荐）

1. **访问Vercel Dashboard**
   - 打开 https://vercel.com/dashboard
   - 点击 "Add New..." → "Project"

2. **导入GitHub仓库**
   - 选择 "Import Git Repository"
   - 找到并选择 `My_Space` 仓库
   - 点击 "Import"

3. **配置项目**
   - **Project Name**: `my-space-frontend` （或其他名称）
   - **Framework Preset**: 选择 "Vite"
   - **Root Directory**: 点击 "Edit"，选择 `frontend`
   - **Build Command**: `npm run build` （默认）
   - **Output Directory**: `dist` （默认）
   - **Install Command**: `npm install` （默认）

4. **部署**
   - 点击 "Deploy" 按钮
   - 等待部署完成（约1-2分钟）
   - 部署成功后会显示前端URL，例如：`https://my-space-frontend.vercel.app`

### 方法2：通过Vercel CLI

1. **安装Vercel CLI**（如果还没安装）
```bash
npm install -g vercel
```

2. **进入前端目录**
```bash
cd frontend
```

3. **登录Vercel**
```bash
vercel login
```

4. **部署**
```bash
vercel --prod
```

5. **按提示操作**
   - Set up and deploy? → Yes
   - Which scope? → 选择你的账号
   - Link to existing project? → No
   - What's your project's name? → my-space-frontend
   - In which directory is your code located? → ./
   - Want to override the settings? → No

6. **等待部署完成**
   - CLI会显示部署进度
   - 完成后会显示前端URL

## 🎯 部署后测试

### 1. 访问前端URL
打开浏览器，访问Vercel提供的URL，例如：
```
https://my-space-frontend.vercel.app
```

### 2. 测试功能
1. **填写岗位需求**
   - 职位名称：Python后端工程师
   - 经验要求：3-5年
   - 技能要求：Python, FastAPI, MySQL
   - 职位描述：负责后端开发...

2. **上传简历**
   - 点击上传区域
   - 选择一个或多个PDF简历
   - 等待AI分析

3. **查看结果**
   - 查看候选人列表（按评分排序）
   - 点击候选人查看详情
   - 查看PDF预览

## 🔧 如果遇到问题

### 问题1：前端无法连接后端API

**症状**：上传简历时报错 "Network Error" 或 "CORS Error"

**解决方案**：
1. 检查 `frontend/src/App.jsx` 中的 `API_BASE_URL` 是否正确
2. 应该是：`https://my-space-beryl.vercel.app`
3. 如果修改了，需要重新部署前端

### 问题2：页面显示空白

**症状**：访问前端URL显示空白页面

**解决方案**：
1. 打开浏览器开发者工具（F12）
2. 查看Console标签是否有错误
3. 检查Network标签，看是否有404错误
4. 确认 `frontend/vite.config.js` 中的 `base` 设置为 `/`

### 问题3：路由404错误

**症状**：刷新页面时显示404

**解决方案**：
1. 确认 `frontend/vercel.json` 文件存在
2. 内容应该是：
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

## 📝 部署后的URL结构

部署完成后，你将有两个URL：

1. **后端API**: https://my-space-beryl.vercel.app
   - 用于处理简历上传和AI分析
   - 不需要直接访问

2. **前端界面**: https://my-space-frontend.vercel.app （或你设置的名称）
   - 这是用户访问的主界面
   - 在这里上传简历和查看结果

## 🎊 完成！

部署完成后：
1. ✅ 访问前端URL查看界面
2. ✅ 测试上传简历功能
3. ✅ 查看AI分析结果
4. ✅ 分享给其他人使用

## 💡 提示

- 前端和后端是分开部署的两个项目
- 前端通过API调用后端服务
- 两个项目都会自动从GitHub同步更新
- 每次push代码到GitHub，Vercel会自动重新部署

## 📚 相关文档

- 后端部署文档：`部署成功.md`
- Vercel官方文档：https://vercel.com/docs
- Vite官方文档：https://vitejs.dev/
